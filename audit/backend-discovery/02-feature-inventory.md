# 02 — User-facing feature inventory

Status legend: **implemented and wired** = full path UI→API→DB→read-back · **saves but not read** = persists, nothing consumes it · other values as defined in the audit brief.

| Feature | User-facing label | Route / component | Backend endpoint | DB model | Auth | Status | Notes |
|---|---|---|---|---|---|---|---|
| Terminal (map, tape, hot, news, brief) | "AI Terminal" | /terminal (`terminal.html`) | none (static JSON) | — | no | **implemented and wired** | Same render for every visitor. Reads only `santro-theme` from localStorage (`terminal.html:1835`). |
| Terminal customization — toggles/view/theme/tickers | **"Customize your terminal — choose what shows up, your default view and theme. Saved to your account."** | /dashboard (`accounts/auth-pages.js:185-197`) | GET/PATCH `/account/preferences` ✅ | `User.preferences` JSON ✅ | yes | **saves but not read** ⚠️ P0 | Write path works end-to-end (backend tests 16/16 pass). **No page ever applies these prefs — terminal.html never fetches them.** Live probe: /terminal makes zero preference reads. |
| Terminal calibration (2nd customization UI) | "Exposure Check — calibrate your terminal", "the output is a configured terminal snapshot" | /quiz (`quiz.html:437-500`) | none — **localStorage only** (`santro_calibration`) | — | yes (gated) | **local only / saves but not read** ⚠️ P0 | Saved key is read by NOTHING (repo-wide grep: writes in quiz.html, zero readers). Doesn't even go to `/account/preferences`, which exists. |
| Watchlist (pin/unpin/list) | "★ Pin" / "My watchlist" | ☆ Pin inside calculator card (`accounts/accounts.js:292,465-476`); modal (`accounts.js:477-…`) | GET/POST/DELETE `/watchlist` ✅ | `WatchlistItem` ✅ | yes | **implemented and wired — but entry point near-invisible** | Pin renders ONLY inside the fair-value calc card, ONLY when logged in, on 3-4 pages (terminal detail panel, /t, /tools/fair-value-calculator). **No "add ticker" input exists anywhere** (see 03). |
| Watchlist → terminal integration | "Your list opens with the terminal, sorted by heat" (`index.html:1051`) | — | — | — | — | **missing** ⚠️ P0 copy | Homepage promise. Terminal never fetches /watchlist (live probe: zero watchlist reads). "Save up to 30 tickers": no cap exists in backend either. |
| Alerts | 🔔 in watchlist modal | `accounts.js:532-620` | full CRUD `/alerts` ✅ | `Alert` ✅ | yes | **implemented and wired** | Honest copy: "checked while this site is open". Server evaluator + push exist for the future mobile app. |
| Saved valuations | "🕘 Saved valuations" | `accounts.js:495-…` | `/valuations` CRUD ✅ | `SavedValuation` ✅ | yes | **implemented and wired** | List/save/delete round-trip present. |
| Fair-value calculator | "Fair-value calculator" | terminal detail panel, /t, /tools/fair-value-calculator (`SantroCalc.render`) | `/usage/status`, `/usage/run` ✅; math = **local** | `UsageEvent/Counter` ✅ | anon 5 runs, then account | **implemented and wired** (metering real, math client-side) | `valuationProvider:"mock"` in prod because **backend `/valuation/run` does not exist** (frontend references it, `api.js:276`). Math itself is real DCF/PE/Graham/PEG in JS — "mock" = provider name, not fake numbers. |
| Saved stress tests | "Free account: … **saved stress tests**" (`index.html:1086`) | — | **none** | **none** | — | **missing / fake promise** ⚠️ P0 copy | No model, no endpoint, no UI. Grep backend for "stress": zero. |
| Stress test | "Portfolio stress test" | /bubble (`bubble.html`, `stress-test.js`) | none | — | partially gated | **implemented and wired (client-only)** | Results not persistable anywhere. |
| Share cards | "Share cards" | /share, /bubble drawCard v3 | none | — | export gated free | **implemented and wired (client-only)** | Canvas export; no persistence by design. |
| Sticker / widget / layout system | — | — | — | — | — | **does not exist** | "sticker": 0 occurrences in both repos. See 03. |
| Locked components / gating | blur + "Create free account" modal | `ds-v2/gates.js`, `locks.js`, homepage `[data-lock]` ×12 | — | — | — | **implemented and wired** (homepage) | Live-verified: modal opens, title "Unlock the full table", CTAs /signup /signin. `save_view`/`map_save_view` defined in gates+copy but **no UI uses them** (dead config). Terminal itself has 0 `[data-lock]` (grandfathered free — intentional). |
| Onboarding profile | "Your profile" (names, status, experience, interest) | /dashboard | PATCH `/account/profile` ✅ | `User` cols ✅ | yes | **implemented and wired** | Greeting uses first_name. Otherwise cosmetic. |
| Old quiz profile chip | "Your Santro market profile: …" | /dashboard (`auth-pages.js:136-149`) | — | — | yes | **dead** | Reads localStorage `santro_quiz` — written only by the DELETED old quiz. Current quiz writes `santro_calibration` (different key, different shape). Chip permanently shows "Take the 60-second check". Has a `TODO(v2)` comment. |
| Account settings (theme pref) | "Theme: Match system / Light / Dark" | /dashboard prefs form | PATCH `/account/preferences` ✅ | `User.preferences` ✅ | yes | **saves but not read** | Site theme is `localStorage santro-theme` via `window.SantroTheme` (nav.js). Account theme is never applied → two competing theme stores. |
| Anonymous metering | "5 free runs" | `api.js` anon id → `X-Santro-Anon` | `/usage/*` ✅ | `UsageCounter` ✅ | no | **implemented and wired** | Live-verified 200s. |
| Auth (register/login/logout/refresh) | "Sign in / Create free account" | signup/signin + modal | `/auth/*` ✅ | User, RefreshToken ✅ | — | **implemented and wired** | Email+password only, per capability discovery. Google/magic dormant by design (never rendered). |
| GDPR export / delete | (API-level) | no UI | `/account/export`, `DELETE /account/` ✅ | all | yes | **backend only** | No frontend surface yet. |
| Devices / notifications inbox | (for iOS app) | no web UI | `/devices`, `/notifications` ✅ | Device, Notification ✅ | yes | **backend only** | Built for the Expo/APNs app (blocked on Apple Developer enrollment). |
| Analytics events | — | `ds-v2/events.js` | **none** | — | — | **local only** | localStorage queue capped 200; "swap the sink for a backend endpoint later". |
| Email brief / iOS app | "Coming soon" | nav/footer | — | — | — | honest placeholder | Correctly labeled, not fake. |
