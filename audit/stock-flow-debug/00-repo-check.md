# 00 — Repo check (stock-flow debug)

2026-07-08 · `/tmp/lk` fresh clone of `lilaburlabur-ux/santro-ai` @ main `b863c2e` (synced to origin; only bot-churned qa report files dirty, restored). package.json = dev tooling (playwright/stylelint/qa gates). Production repo confirmed — proceeding.

Related state: prefs→terminal wiring live (9f31d81), theme integrity live (8cf4d01). Backend `santro-accounts` untouched by this pass (watchlist/alerts/valuations APIs already exist and are used).
